class Controller:

    def ask_for_command(self):
        if self.view.type == "event":
            return self.view.ask_for_command()

        choice = input()
        # trouve la commande liée au choix


class CreatePlayerView(EventView):

    display(self): """Affiche le titre de la page etc."""
        pass


    def ask_for_command(self):
        player_data = {}
        player_data[
    "name"
] = input("le nom svp")
        # autres inputs

        return CreatePlayerCommand(**player_data)